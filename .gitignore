# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# The raw zone and the DuckDB archive. Anchored to the repository root: a bare
# `data/` also matches tests/data/, which holds the fixtures the suite needs.
/data/
